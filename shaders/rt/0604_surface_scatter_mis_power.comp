#version 460

// 06 Sample Surface Scattering - MULTIPLE IMPORTANCE SAMPLING, POWER HEURISTIC. A light sample and a
// BSDF sample, each weighted by its share of the squared densities (PBR 4ed eq. 2.15, exponent 2),
// which trusts the better technique more than the balance heuristic does.
// The body is crt_surface.glsl; see there for what the three strategies share.

#extension GL_GOOGLE_include_directive : require
#define CRT_DIRECT_LIGHTING 2
#define CRT_MIS_EXPONENT 2.0
#include "include/crt_common.glsl"
#include "include/crt_random.glsl"
#include "equirect.glsl"
#include "include/crt_scatter.glsl"
#include "include/crt_light.glsl"
#include "include/crt_debug.glsl"
#include "include/crt_surface.glsl"
